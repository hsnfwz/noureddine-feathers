<script lang="ts">

  // props
  // export let name: string = '';


  // // props with default values;
  // // export let name = 'Hussein';

  // // variables
  // let numbers = [1, 2, 3, 4];
  // let country = 'Canada';

  // // assignments (state)
  // let count = 0;

  // // statements (side effects)
  // $: double = count * 2;
  // $: sum = numbers.reduce((t, n) => t + n, 0);

  // // declarations (side effects)
  // $: {
  //   console.log('hi', name)
  //   console.log('count', count);
  // }

  // // functions
  // const addNumber = () => numbers = [ ...numbers, numbers.length + 1 ];

</script>

<!-- <input bind:value={country}>
<p>Your country is: {country}</p>

<h1>{name}: c({count}) d({double})</h1>
<button type="button" on:click={() => count = count + 1}>+1</button>

<p>{numbers.join(' + ')} = {sum}</p>

<button on:click={addNumber}>
	Add a number
</button>

{#if count === 10}
  <p>Count is now 10</p>
{:else}
  <p>Count in progress...</p>
{/if}

{#each numbers as number, index}
  <p>My number is {number} and index is {index}</p>
{/each} -->